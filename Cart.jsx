import ".Cart.css";

function Cart() {
    return (
        <div className="cart page">
            <h1>Got Everything?</h1>
            <h4>Let's process to checkout</h4>
        </div>
    );
}